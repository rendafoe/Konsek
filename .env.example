# Konsek Development Environment
# Copy this file to .env and fill in your values: cp .env.example .env

# Database (Supabase PostgreSQL)
DATABASE_URL=postgresql://user:password@host:5432/postgres

# Server Port (macOS uses 5000 for AirPlay, so we use 3001)
PORT=3001

# Strava OAuth (get these from https://www.strava.com/settings/api)
STRAVA_CLIENT_ID=your_client_id
STRAVA_CLIENT_SECRET=your_client_secret

# OpenWeatherMap API (for weather-based special rewards)
# Get a free API key at https://openweathermap.org/api
OPENWEATHERMAP_API_KEY=your_api_key

# NextAuth.js (generate with: openssl rand -base64 32)
NEXTAUTH_SECRET=your_secret_here
NEXTAUTH_URL=http://localhost:3000

# Google OAuth (optional, for production auth)
# GOOGLE_CLIENT_ID=your_google_client_id
# GOOGLE_CLIENT_SECRET=your_google_client_secret
