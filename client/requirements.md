## Packages
framer-motion | For smooth animations and transitions
recharts | For visualizing running stats
date-fns | For date formatting and manipulation

## Notes
- Tailwind Config: Add 'Press Start 2P' to font families.
- Auth: Use existing `useAuth` hook. Redirect to `/api/login`.
- Strava: OAuth flow via `/api/strava/connect`.
- Images: Using CSS/SVG for pixel art placeholders initially.
